import Query from "./query.js";

class PerformerQuery extends Query{
    getPerformerByName = (performerName) => {
        const performerTable = this.schema.performerTable;
        return`
        
        `
    }
}

export default PerformerQuery;